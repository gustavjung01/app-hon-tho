import type { Express } from "express";

export function registerStrictConversationCreateRoute(_app: Express) {
  // Legacy compatibility shim. The real POST /api/conversations route lives in conversationRoutes.ts.
}
